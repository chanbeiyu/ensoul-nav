version: '3'

services:
  onenav:
    image: findstay/ensoul-nav:latest
    container_name: ensoul-nav
    privileged: true
    restart: always
    ports:
      - "9000:9000"
    volumes:
      - './:/app'
      # - '${WEBAPP_STORAGE_HOME}/:/app'
    environment:
      TZ: Asia/Shanghai

# https://gitee.com/tznb/TwoNav
